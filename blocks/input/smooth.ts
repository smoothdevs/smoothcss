import { Preset } from '../types';

const smooth: Preset = {
  id: 'smooth',
  name: 'Smooth',
  styles: {
    root: {
      'padding': '4px',
      'font-size': '16px',
    },
    hover: {},
    active: {},
  },
};

export default smooth;
