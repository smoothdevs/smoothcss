import { Preset } from '../types';

const smooth: Preset = {
  id: 'smooth',
  name: 'smooth',
  styles: {
    root: {
      'padding': '4px',
      'font-size': '16px',
    },
    hover: {},
  },
};

export default smooth;
